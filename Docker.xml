https://docs.docker.com/engine/swarm/swarm-tutorial/


docker exec -i -t mysql /bin/bash

docker run --name mysql -e MYSQL_ROOT_PASSWORD=root -e MYSQL_DATABASE=donation  -p 3306:3306 -d mysql:latest


version: '3.1'

services:

    db:
        image: mysql
        environment:
            MYSQL_ROOT_PASSWORD: root
            MYSQL_DATABASE: donation